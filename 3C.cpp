#include <stdio.h>

int main() {
    int x=6;
    int y=2;
    int s=x+y;
    int sub=x-y;
    int m=x*y;
    printf("A soma, a subtracao e a multiplicacao dos 2 numeros sao: %i, %i, %i", s, sub, m);

    return 0;
}
