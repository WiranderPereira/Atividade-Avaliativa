#include <stdio.h>

int main() {
    int x=6;
    int y=2;
    int w=3;
    int s=x+y+w;
    int sub=x-y-w;
    int m=x*y*w;
    printf("A soma, a subtracao e a multiplicacao dos 3 numeros sao: %i, %i, %i", s, sub, m);

    return 0;
}
