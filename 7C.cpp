#include <stdio.h>

int main() {
     float x=6;
     float y=2;
     float w=3;
     float s=x+y+w;
     float sub=x-y-w;
     float m=x*y*w;
    float d=x/y/w;
    printf("A soma, a subtracao, a multiplicacao e a divisao dos 3 numeros sao: %f, %f, %f, %f", s, sub, m, d);

    return 0;
} 
