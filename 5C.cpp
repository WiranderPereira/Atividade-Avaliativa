#include <stdio.h>

int main() {
    int x=6;
    int y=2;
    int s=x+y;
    int sub=x-y;
    int m=x*y;
    int d=x/y;
    printf("A soma, a subtracao, a multiplicacao e a divisao dos 2 numeros sao: %i, %i, %i, %i", s, sub, m, d);

    return 0;
}
