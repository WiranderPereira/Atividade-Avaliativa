#include <stdio.h>

int main() {
    int a=6;
    int b=2;
    int c=3;
    int soma=(a*a)+(b*b)+(c*c);
    printf("A soma entre os tres quadrados dos numeros sao: %i", soma);

    return 0;
}
